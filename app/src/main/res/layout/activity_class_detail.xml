<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvInstruction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tap a student to mark attendance"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerStudents"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/tvInstruction"
        android:paddingBottom="120dp"/>

    <ImageButton
        android:id="@+id/fabAddStudent"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_margin="16dp"
        android:background="@null"
        android:src="@drawable/plus"
        android:contentDescription="Add Student"/>

    <ImageButton
        android:id="@+id/fabStart"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_margin="16dp"
        android:background="@null"
        android:src="@drawable/startattendance"
        android:contentDescription="Start Attendance"/>

    <ImageButton
        android:id="@+id/btnHoliday"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="16dp"
        android:background="@null"
        android:src="@drawable/holiday"
        android:contentDescription="Holiday"/>

    <ImageButton
        android:id="@+id/btnData"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_above="@id/btnHoliday"
        android:layout_centerHorizontal="true"
        android:background="@null"
        android:src="@drawable/paper"
        android:contentDescription="View Data"/>
</RelativeLayout>
